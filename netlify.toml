# Default site = public portal (Package directory empty / repo root).

[build]
  command = "npx nx run public-portal:build --configuration=dev"
  publish = "dist/apps/public-portal/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
